[pytest]
# Lancer depuis la racine du projet :  python -m pytest
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning:starlette.testclient
    # Lecteurs de documents encore dans langchain-community : migration notée dans le README
    ignore::DeprecationWarning:src.ingestion
